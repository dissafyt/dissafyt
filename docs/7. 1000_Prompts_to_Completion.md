1.  Copilot task: In the Project Setup phase, Create Django model with
    clear comments and minimal dependencies.
2.  Copilot task: In the Project Setup phase, Create Django form with
    clear comments and minimal dependencies.
3.  Copilot task: In the Project Setup phase, Create Django view with
    clear comments and minimal dependencies.
4.  Copilot task: In the Project Setup phase, Create HTML template with
    clear comments and minimal dependencies.
5.  Copilot task: In the Project Setup phase, Create URL route with
    clear comments and minimal dependencies.
6.  Copilot task: In the Project Setup phase, Add admin registration
    with clear comments and minimal dependencies.
7.  Copilot task: In the Project Setup phase, Write validation logic
    with clear comments and minimal dependencies.
8.  Copilot task: In the Project Setup phase, Add Tailwind styling with
    clear comments and minimal dependencies.
9.  Copilot task: In the Project Setup phase, Write helper function with
    clear comments and minimal dependencies.
10. Copilot task: In the Project Setup phase, Write unit test with clear
    comments and minimal dependencies.
11. Copilot task: In the Django Configuration phase, Create Django model
    with clear comments and minimal dependencies.
12. Copilot task: In the Django Configuration phase, Create Django form
    with clear comments and minimal dependencies.
13. Copilot task: In the Django Configuration phase, Create Django view
    with clear comments and minimal dependencies.
14. Copilot task: In the Django Configuration phase, Create HTML
    template with clear comments and minimal dependencies.
15. Copilot task: In the Django Configuration phase, Create URL route
    with clear comments and minimal dependencies.
16. Copilot task: In the Django Configuration phase, Add admin
    registration with clear comments and minimal dependencies.
17. Copilot task: In the Django Configuration phase, Write validation
    logic with clear comments and minimal dependencies.
18. Copilot task: In the Django Configuration phase, Add Tailwind
    styling with clear comments and minimal dependencies.
19. Copilot task: In the Django Configuration phase, Write helper
    function with clear comments and minimal dependencies.
20. Copilot task: In the Django Configuration phase, Write unit test
    with clear comments and minimal dependencies.
21. Copilot task: In the Marketing Website phase, Create Django model
    with clear comments and minimal dependencies.
22. Copilot task: In the Marketing Website phase, Create Django form
    with clear comments and minimal dependencies.
23. Copilot task: In the Marketing Website phase, Create Django view
    with clear comments and minimal dependencies.
24. Copilot task: In the Marketing Website phase, Create HTML template
    with clear comments and minimal dependencies.
25. Copilot task: In the Marketing Website phase, Create URL route with
    clear comments and minimal dependencies.
26. Copilot task: In the Marketing Website phase, Add admin registration
    with clear comments and minimal dependencies.
27. Copilot task: In the Marketing Website phase, Write validation logic
    with clear comments and minimal dependencies.
28. Copilot task: In the Marketing Website phase, Add Tailwind styling
    with clear comments and minimal dependencies.
29. Copilot task: In the Marketing Website phase, Write helper function
    with clear comments and minimal dependencies.
30. Copilot task: In the Marketing Website phase, Write unit test with
    clear comments and minimal dependencies.
31. Copilot task: In the Authentication System phase, Create Django
    model with clear comments and minimal dependencies.
32. Copilot task: In the Authentication System phase, Create Django form
    with clear comments and minimal dependencies.
33. Copilot task: In the Authentication System phase, Create Django view
    with clear comments and minimal dependencies.
34. Copilot task: In the Authentication System phase, Create HTML
    template with clear comments and minimal dependencies.
35. Copilot task: In the Authentication System phase, Create URL route
    with clear comments and minimal dependencies.
36. Copilot task: In the Authentication System phase, Add admin
    registration with clear comments and minimal dependencies.
37. Copilot task: In the Authentication System phase, Write validation
    logic with clear comments and minimal dependencies.
38. Copilot task: In the Authentication System phase, Add Tailwind
    styling with clear comments and minimal dependencies.
39. Copilot task: In the Authentication System phase, Write helper
    function with clear comments and minimal dependencies.
40. Copilot task: In the Authentication System phase, Write unit test
    with clear comments and minimal dependencies.
41. Copilot task: In the Dashboard UI phase, Create Django model with
    clear comments and minimal dependencies.
42. Copilot task: In the Dashboard UI phase, Create Django form with
    clear comments and minimal dependencies.
43. Copilot task: In the Dashboard UI phase, Create Django view with
    clear comments and minimal dependencies.
44. Copilot task: In the Dashboard UI phase, Create HTML template with
    clear comments and minimal dependencies.
45. Copilot task: In the Dashboard UI phase, Create URL route with clear
    comments and minimal dependencies.
46. Copilot task: In the Dashboard UI phase, Add admin registration with
    clear comments and minimal dependencies.
47. Copilot task: In the Dashboard UI phase, Write validation logic with
    clear comments and minimal dependencies.
48. Copilot task: In the Dashboard UI phase, Add Tailwind styling with
    clear comments and minimal dependencies.
49. Copilot task: In the Dashboard UI phase, Write helper function with
    clear comments and minimal dependencies.
50. Copilot task: In the Dashboard UI phase, Write unit test with clear
    comments and minimal dependencies.
51. Copilot task: In the CMS Models phase, Create Django model with
    clear comments and minimal dependencies.
52. Copilot task: In the CMS Models phase, Create Django form with clear
    comments and minimal dependencies.
53. Copilot task: In the CMS Models phase, Create Django view with clear
    comments and minimal dependencies.
54. Copilot task: In the CMS Models phase, Create HTML template with
    clear comments and minimal dependencies.
55. Copilot task: In the CMS Models phase, Create URL route with clear
    comments and minimal dependencies.
56. Copilot task: In the CMS Models phase, Add admin registration with
    clear comments and minimal dependencies.
57. Copilot task: In the CMS Models phase, Write validation logic with
    clear comments and minimal dependencies.
58. Copilot task: In the CMS Models phase, Add Tailwind styling with
    clear comments and minimal dependencies.
59. Copilot task: In the CMS Models phase, Write helper function with
    clear comments and minimal dependencies.
60. Copilot task: In the CMS Models phase, Write unit test with clear
    comments and minimal dependencies.
61. Copilot task: In the CMS Forms phase, Create Django model with clear
    comments and minimal dependencies.
62. Copilot task: In the CMS Forms phase, Create Django form with clear
    comments and minimal dependencies.
63. Copilot task: In the CMS Forms phase, Create Django view with clear
    comments and minimal dependencies.
64. Copilot task: In the CMS Forms phase, Create HTML template with
    clear comments and minimal dependencies.
65. Copilot task: In the CMS Forms phase, Create URL route with clear
    comments and minimal dependencies.
66. Copilot task: In the CMS Forms phase, Add admin registration with
    clear comments and minimal dependencies.
67. Copilot task: In the CMS Forms phase, Write validation logic with
    clear comments and minimal dependencies.
68. Copilot task: In the CMS Forms phase, Add Tailwind styling with
    clear comments and minimal dependencies.
69. Copilot task: In the CMS Forms phase, Write helper function with
    clear comments and minimal dependencies.
70. Copilot task: In the CMS Forms phase, Write unit test with clear
    comments and minimal dependencies.
71. Copilot task: In the CMS Views phase, Create Django model with clear
    comments and minimal dependencies.
72. Copilot task: In the CMS Views phase, Create Django form with clear
    comments and minimal dependencies.
73. Copilot task: In the CMS Views phase, Create Django view with clear
    comments and minimal dependencies.
74. Copilot task: In the CMS Views phase, Create HTML template with
    clear comments and minimal dependencies.
75. Copilot task: In the CMS Views phase, Create URL route with clear
    comments and minimal dependencies.
76. Copilot task: In the CMS Views phase, Add admin registration with
    clear comments and minimal dependencies.
77. Copilot task: In the CMS Views phase, Write validation logic with
    clear comments and minimal dependencies.
78. Copilot task: In the CMS Views phase, Add Tailwind styling with
    clear comments and minimal dependencies.
79. Copilot task: In the CMS Views phase, Write helper function with
    clear comments and minimal dependencies.
80. Copilot task: In the CMS Views phase, Write unit test with clear
    comments and minimal dependencies.
81. Copilot task: In the Website Instance System phase, Create Django
    model with clear comments and minimal dependencies.
82. Copilot task: In the Website Instance System phase, Create Django
    form with clear comments and minimal dependencies.
83. Copilot task: In the Website Instance System phase, Create Django
    view with clear comments and minimal dependencies.
84. Copilot task: In the Website Instance System phase, Create HTML
    template with clear comments and minimal dependencies.
85. Copilot task: In the Website Instance System phase, Create URL route
    with clear comments and minimal dependencies.
86. Copilot task: In the Website Instance System phase, Add admin
    registration with clear comments and minimal dependencies.
87. Copilot task: In the Website Instance System phase, Write validation
    logic with clear comments and minimal dependencies.
88. Copilot task: In the Website Instance System phase, Add Tailwind
    styling with clear comments and minimal dependencies.
89. Copilot task: In the Website Instance System phase, Write helper
    function with clear comments and minimal dependencies.
90. Copilot task: In the Website Instance System phase, Write unit test
    with clear comments and minimal dependencies.
91. Copilot task: In the Template Rendering phase, Create Django model
    with clear comments and minimal dependencies.
92. Copilot task: In the Template Rendering phase, Create Django form
    with clear comments and minimal dependencies.
93. Copilot task: In the Template Rendering phase, Create Django view
    with clear comments and minimal dependencies.
94. Copilot task: In the Template Rendering phase, Create HTML template
    with clear comments and minimal dependencies.
95. Copilot task: In the Template Rendering phase, Create URL route with
    clear comments and minimal dependencies.
96. Copilot task: In the Template Rendering phase, Add admin
    registration with clear comments and minimal dependencies.
97. Copilot task: In the Template Rendering phase, Write validation
    logic with clear comments and minimal dependencies.
98. Copilot task: In the Template Rendering phase, Add Tailwind styling
    with clear comments and minimal dependencies.
99. Copilot task: In the Template Rendering phase, Write helper function
    with clear comments and minimal dependencies.
100. Copilot task: In the Template Rendering phase, Write unit test with
     clear comments and minimal dependencies.
101. Copilot task: In the Subdomain Routing phase, Create Django model
     with clear comments and minimal dependencies.
102. Copilot task: In the Subdomain Routing phase, Create Django form
     with clear comments and minimal dependencies.
103. Copilot task: In the Subdomain Routing phase, Create Django view
     with clear comments and minimal dependencies.
104. Copilot task: In the Subdomain Routing phase, Create HTML template
     with clear comments and minimal dependencies.
105. Copilot task: In the Subdomain Routing phase, Create URL route with
     clear comments and minimal dependencies.
106. Copilot task: In the Subdomain Routing phase, Add admin
     registration with clear comments and minimal dependencies.
107. Copilot task: In the Subdomain Routing phase, Write validation
     logic with clear comments and minimal dependencies.
108. Copilot task: In the Subdomain Routing phase, Add Tailwind styling
     with clear comments and minimal dependencies.
109. Copilot task: In the Subdomain Routing phase, Write helper function
     with clear comments and minimal dependencies.
110. Copilot task: In the Subdomain Routing phase, Write unit test with
     clear comments and minimal dependencies.
111. Copilot task: In the Onboarding Wizard phase, Create Django model
     with clear comments and minimal dependencies.
112. Copilot task: In the Onboarding Wizard phase, Create Django form
     with clear comments and minimal dependencies.
113. Copilot task: In the Onboarding Wizard phase, Create Django view
     with clear comments and minimal dependencies.
114. Copilot task: In the Onboarding Wizard phase, Create HTML template
     with clear comments and minimal dependencies.
115. Copilot task: In the Onboarding Wizard phase, Create URL route with
     clear comments and minimal dependencies.
116. Copilot task: In the Onboarding Wizard phase, Add admin
     registration with clear comments and minimal dependencies.
117. Copilot task: In the Onboarding Wizard phase, Write validation
     logic with clear comments and minimal dependencies.
118. Copilot task: In the Onboarding Wizard phase, Add Tailwind styling
     with clear comments and minimal dependencies.
119. Copilot task: In the Onboarding Wizard phase, Write helper function
     with clear comments and minimal dependencies.
120. Copilot task: In the Onboarding Wizard phase, Write unit test with
     clear comments and minimal dependencies.
121. Copilot task: In the Template Library phase, Create Django model
     with clear comments and minimal dependencies.
122. Copilot task: In the Template Library phase, Create Django form
     with clear comments and minimal dependencies.
123. Copilot task: In the Template Library phase, Create Django view
     with clear comments and minimal dependencies.
124. Copilot task: In the Template Library phase, Create HTML template
     with clear comments and minimal dependencies.
125. Copilot task: In the Template Library phase, Create URL route with
     clear comments and minimal dependencies.
126. Copilot task: In the Template Library phase, Add admin registration
     with clear comments and minimal dependencies.
127. Copilot task: In the Template Library phase, Write validation logic
     with clear comments and minimal dependencies.
128. Copilot task: In the Template Library phase, Add Tailwind styling
     with clear comments and minimal dependencies.
129. Copilot task: In the Template Library phase, Write helper function
     with clear comments and minimal dependencies.
130. Copilot task: In the Template Library phase, Write unit test with
     clear comments and minimal dependencies.
131. Copilot task: In the Media Handling phase, Create Django model with
     clear comments and minimal dependencies.
132. Copilot task: In the Media Handling phase, Create Django form with
     clear comments and minimal dependencies.
133. Copilot task: In the Media Handling phase, Create Django view with
     clear comments and minimal dependencies.
134. Copilot task: In the Media Handling phase, Create HTML template
     with clear comments and minimal dependencies.
135. Copilot task: In the Media Handling phase, Create URL route with
     clear comments and minimal dependencies.
136. Copilot task: In the Media Handling phase, Add admin registration
     with clear comments and minimal dependencies.
137. Copilot task: In the Media Handling phase, Write validation logic
     with clear comments and minimal dependencies.
138. Copilot task: In the Media Handling phase, Add Tailwind styling
     with clear comments and minimal dependencies.
139. Copilot task: In the Media Handling phase, Write helper function
     with clear comments and minimal dependencies.
140. Copilot task: In the Media Handling phase, Write unit test with
     clear comments and minimal dependencies.
141. Copilot task: In the Domain Management phase, Create Django model
     with clear comments and minimal dependencies.
142. Copilot task: In the Domain Management phase, Create Django form
     with clear comments and minimal dependencies.
143. Copilot task: In the Domain Management phase, Create Django view
     with clear comments and minimal dependencies.
144. Copilot task: In the Domain Management phase, Create HTML template
     with clear comments and minimal dependencies.
145. Copilot task: In the Domain Management phase, Create URL route with
     clear comments and minimal dependencies.
146. Copilot task: In the Domain Management phase, Add admin
     registration with clear comments and minimal dependencies.
147. Copilot task: In the Domain Management phase, Write validation
     logic with clear comments and minimal dependencies.
148. Copilot task: In the Domain Management phase, Add Tailwind styling
     with clear comments and minimal dependencies.
149. Copilot task: In the Domain Management phase, Write helper function
     with clear comments and minimal dependencies.
150. Copilot task: In the Domain Management phase, Write unit test with
     clear comments and minimal dependencies.
151. Copilot task: In the Admin Tools phase, Create Django model with
     clear comments and minimal dependencies.
152. Copilot task: In the Admin Tools phase, Create Django form with
     clear comments and minimal dependencies.
153. Copilot task: In the Admin Tools phase, Create Django view with
     clear comments and minimal dependencies.
154. Copilot task: In the Admin Tools phase, Create HTML template with
     clear comments and minimal dependencies.
155. Copilot task: In the Admin Tools phase, Create URL route with clear
     comments and minimal dependencies.
156. Copilot task: In the Admin Tools phase, Add admin registration with
     clear comments and minimal dependencies.
157. Copilot task: In the Admin Tools phase, Write validation logic with
     clear comments and minimal dependencies.
158. Copilot task: In the Admin Tools phase, Add Tailwind styling with
     clear comments and minimal dependencies.
159. Copilot task: In the Admin Tools phase, Write helper function with
     clear comments and minimal dependencies.
160. Copilot task: In the Admin Tools phase, Write unit test with clear
     comments and minimal dependencies.
161. Copilot task: In the Testing phase, Create Django model with clear
     comments and minimal dependencies.
162. Copilot task: In the Testing phase, Create Django form with clear
     comments and minimal dependencies.
163. Copilot task: In the Testing phase, Create Django view with clear
     comments and minimal dependencies.
164. Copilot task: In the Testing phase, Create HTML template with clear
     comments and minimal dependencies.
165. Copilot task: In the Testing phase, Create URL route with clear
     comments and minimal dependencies.
166. Copilot task: In the Testing phase, Add admin registration with
     clear comments and minimal dependencies.
167. Copilot task: In the Testing phase, Write validation logic with
     clear comments and minimal dependencies.
168. Copilot task: In the Testing phase, Add Tailwind styling with clear
     comments and minimal dependencies.
169. Copilot task: In the Testing phase, Write helper function with
     clear comments and minimal dependencies.
170. Copilot task: In the Testing phase, Write unit test with clear
     comments and minimal dependencies.
171. Copilot task: In the Deployment Preparation phase, Create Django
     model with clear comments and minimal dependencies.
172. Copilot task: In the Deployment Preparation phase, Create Django
     form with clear comments and minimal dependencies.
173. Copilot task: In the Deployment Preparation phase, Create Django
     view with clear comments and minimal dependencies.
174. Copilot task: In the Deployment Preparation phase, Create HTML
     template with clear comments and minimal dependencies.
175. Copilot task: In the Deployment Preparation phase, Create URL route
     with clear comments and minimal dependencies.
176. Copilot task: In the Deployment Preparation phase, Add admin
     registration with clear comments and minimal dependencies.
177. Copilot task: In the Deployment Preparation phase, Write validation
     logic with clear comments and minimal dependencies.
178. Copilot task: In the Deployment Preparation phase, Add Tailwind
     styling with clear comments and minimal dependencies.
179. Copilot task: In the Deployment Preparation phase, Write helper
     function with clear comments and minimal dependencies.
180. Copilot task: In the Deployment Preparation phase, Write unit test
     with clear comments and minimal dependencies.
181. Copilot task: In the Project Setup phase, Create Django model with
     clear comments and minimal dependencies.
182. Copilot task: In the Project Setup phase, Create Django form with
     clear comments and minimal dependencies.
183. Copilot task: In the Project Setup phase, Create Django view with
     clear comments and minimal dependencies.
184. Copilot task: In the Project Setup phase, Create HTML template with
     clear comments and minimal dependencies.
185. Copilot task: In the Project Setup phase, Create URL route with
     clear comments and minimal dependencies.
186. Copilot task: In the Project Setup phase, Add admin registration
     with clear comments and minimal dependencies.
187. Copilot task: In the Project Setup phase, Write validation logic
     with clear comments and minimal dependencies.
188. Copilot task: In the Project Setup phase, Add Tailwind styling with
     clear comments and minimal dependencies.
189. Copilot task: In the Project Setup phase, Write helper function
     with clear comments and minimal dependencies.
190. Copilot task: In the Project Setup phase, Write unit test with
     clear comments and minimal dependencies.
191. Copilot task: In the Django Configuration phase, Create Django
     model with clear comments and minimal dependencies.
192. Copilot task: In the Django Configuration phase, Create Django form
     with clear comments and minimal dependencies.
193. Copilot task: In the Django Configuration phase, Create Django view
     with clear comments and minimal dependencies.
194. Copilot task: In the Django Configuration phase, Create HTML
     template with clear comments and minimal dependencies.
195. Copilot task: In the Django Configuration phase, Create URL route
     with clear comments and minimal dependencies.
196. Copilot task: In the Django Configuration phase, Add admin
     registration with clear comments and minimal dependencies.
197. Copilot task: In the Django Configuration phase, Write validation
     logic with clear comments and minimal dependencies.
198. Copilot task: In the Django Configuration phase, Add Tailwind
     styling with clear comments and minimal dependencies.
199. Copilot task: In the Django Configuration phase, Write helper
     function with clear comments and minimal dependencies.
200. Copilot task: In the Django Configuration phase, Write unit test
     with clear comments and minimal dependencies.
201. Copilot task: In the Marketing Website phase, Create Django model
     with clear comments and minimal dependencies.
202. Copilot task: In the Marketing Website phase, Create Django form
     with clear comments and minimal dependencies.
203. Copilot task: In the Marketing Website phase, Create Django view
     with clear comments and minimal dependencies.
204. Copilot task: In the Marketing Website phase, Create HTML template
     with clear comments and minimal dependencies.
205. Copilot task: In the Marketing Website phase, Create URL route with
     clear comments and minimal dependencies.
206. Copilot task: In the Marketing Website phase, Add admin
     registration with clear comments and minimal dependencies.
207. Copilot task: In the Marketing Website phase, Write validation
     logic with clear comments and minimal dependencies.
208. Copilot task: In the Marketing Website phase, Add Tailwind styling
     with clear comments and minimal dependencies.
209. Copilot task: In the Marketing Website phase, Write helper function
     with clear comments and minimal dependencies.
210. Copilot task: In the Marketing Website phase, Write unit test with
     clear comments and minimal dependencies.
211. Copilot task: In the Authentication System phase, Create Django
     model with clear comments and minimal dependencies.
212. Copilot task: In the Authentication System phase, Create Django
     form with clear comments and minimal dependencies.
213. Copilot task: In the Authentication System phase, Create Django
     view with clear comments and minimal dependencies.
214. Copilot task: In the Authentication System phase, Create HTML
     template with clear comments and minimal dependencies.
215. Copilot task: In the Authentication System phase, Create URL route
     with clear comments and minimal dependencies.
216. Copilot task: In the Authentication System phase, Add admin
     registration with clear comments and minimal dependencies.
217. Copilot task: In the Authentication System phase, Write validation
     logic with clear comments and minimal dependencies.
218. Copilot task: In the Authentication System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
219. Copilot task: In the Authentication System phase, Write helper
     function with clear comments and minimal dependencies.
220. Copilot task: In the Authentication System phase, Write unit test
     with clear comments and minimal dependencies.
221. Copilot task: In the Dashboard UI phase, Create Django model with
     clear comments and minimal dependencies.
222. Copilot task: In the Dashboard UI phase, Create Django form with
     clear comments and minimal dependencies.
223. Copilot task: In the Dashboard UI phase, Create Django view with
     clear comments and minimal dependencies.
224. Copilot task: In the Dashboard UI phase, Create HTML template with
     clear comments and minimal dependencies.
225. Copilot task: In the Dashboard UI phase, Create URL route with
     clear comments and minimal dependencies.
226. Copilot task: In the Dashboard UI phase, Add admin registration
     with clear comments and minimal dependencies.
227. Copilot task: In the Dashboard UI phase, Write validation logic
     with clear comments and minimal dependencies.
228. Copilot task: In the Dashboard UI phase, Add Tailwind styling with
     clear comments and minimal dependencies.
229. Copilot task: In the Dashboard UI phase, Write helper function with
     clear comments and minimal dependencies.
230. Copilot task: In the Dashboard UI phase, Write unit test with clear
     comments and minimal dependencies.
231. Copilot task: In the CMS Models phase, Create Django model with
     clear comments and minimal dependencies.
232. Copilot task: In the CMS Models phase, Create Django form with
     clear comments and minimal dependencies.
233. Copilot task: In the CMS Models phase, Create Django view with
     clear comments and minimal dependencies.
234. Copilot task: In the CMS Models phase, Create HTML template with
     clear comments and minimal dependencies.
235. Copilot task: In the CMS Models phase, Create URL route with clear
     comments and minimal dependencies.
236. Copilot task: In the CMS Models phase, Add admin registration with
     clear comments and minimal dependencies.
237. Copilot task: In the CMS Models phase, Write validation logic with
     clear comments and minimal dependencies.
238. Copilot task: In the CMS Models phase, Add Tailwind styling with
     clear comments and minimal dependencies.
239. Copilot task: In the CMS Models phase, Write helper function with
     clear comments and minimal dependencies.
240. Copilot task: In the CMS Models phase, Write unit test with clear
     comments and minimal dependencies.
241. Copilot task: In the CMS Forms phase, Create Django model with
     clear comments and minimal dependencies.
242. Copilot task: In the CMS Forms phase, Create Django form with clear
     comments and minimal dependencies.
243. Copilot task: In the CMS Forms phase, Create Django view with clear
     comments and minimal dependencies.
244. Copilot task: In the CMS Forms phase, Create HTML template with
     clear comments and minimal dependencies.
245. Copilot task: In the CMS Forms phase, Create URL route with clear
     comments and minimal dependencies.
246. Copilot task: In the CMS Forms phase, Add admin registration with
     clear comments and minimal dependencies.
247. Copilot task: In the CMS Forms phase, Write validation logic with
     clear comments and minimal dependencies.
248. Copilot task: In the CMS Forms phase, Add Tailwind styling with
     clear comments and minimal dependencies.
249. Copilot task: In the CMS Forms phase, Write helper function with
     clear comments and minimal dependencies.
250. Copilot task: In the CMS Forms phase, Write unit test with clear
     comments and minimal dependencies.
251. Copilot task: In the CMS Views phase, Create Django model with
     clear comments and minimal dependencies.
252. Copilot task: In the CMS Views phase, Create Django form with clear
     comments and minimal dependencies.
253. Copilot task: In the CMS Views phase, Create Django view with clear
     comments and minimal dependencies.
254. Copilot task: In the CMS Views phase, Create HTML template with
     clear comments and minimal dependencies.
255. Copilot task: In the CMS Views phase, Create URL route with clear
     comments and minimal dependencies.
256. Copilot task: In the CMS Views phase, Add admin registration with
     clear comments and minimal dependencies.
257. Copilot task: In the CMS Views phase, Write validation logic with
     clear comments and minimal dependencies.
258. Copilot task: In the CMS Views phase, Add Tailwind styling with
     clear comments and minimal dependencies.
259. Copilot task: In the CMS Views phase, Write helper function with
     clear comments and minimal dependencies.
260. Copilot task: In the CMS Views phase, Write unit test with clear
     comments and minimal dependencies.
261. Copilot task: In the Website Instance System phase, Create Django
     model with clear comments and minimal dependencies.
262. Copilot task: In the Website Instance System phase, Create Django
     form with clear comments and minimal dependencies.
263. Copilot task: In the Website Instance System phase, Create Django
     view with clear comments and minimal dependencies.
264. Copilot task: In the Website Instance System phase, Create HTML
     template with clear comments and minimal dependencies.
265. Copilot task: In the Website Instance System phase, Create URL
     route with clear comments and minimal dependencies.
266. Copilot task: In the Website Instance System phase, Add admin
     registration with clear comments and minimal dependencies.
267. Copilot task: In the Website Instance System phase, Write
     validation logic with clear comments and minimal dependencies.
268. Copilot task: In the Website Instance System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
269. Copilot task: In the Website Instance System phase, Write helper
     function with clear comments and minimal dependencies.
270. Copilot task: In the Website Instance System phase, Write unit test
     with clear comments and minimal dependencies.
271. Copilot task: In the Template Rendering phase, Create Django model
     with clear comments and minimal dependencies.
272. Copilot task: In the Template Rendering phase, Create Django form
     with clear comments and minimal dependencies.
273. Copilot task: In the Template Rendering phase, Create Django view
     with clear comments and minimal dependencies.
274. Copilot task: In the Template Rendering phase, Create HTML template
     with clear comments and minimal dependencies.
275. Copilot task: In the Template Rendering phase, Create URL route
     with clear comments and minimal dependencies.
276. Copilot task: In the Template Rendering phase, Add admin
     registration with clear comments and minimal dependencies.
277. Copilot task: In the Template Rendering phase, Write validation
     logic with clear comments and minimal dependencies.
278. Copilot task: In the Template Rendering phase, Add Tailwind styling
     with clear comments and minimal dependencies.
279. Copilot task: In the Template Rendering phase, Write helper
     function with clear comments and minimal dependencies.
280. Copilot task: In the Template Rendering phase, Write unit test with
     clear comments and minimal dependencies.
281. Copilot task: In the Subdomain Routing phase, Create Django model
     with clear comments and minimal dependencies.
282. Copilot task: In the Subdomain Routing phase, Create Django form
     with clear comments and minimal dependencies.
283. Copilot task: In the Subdomain Routing phase, Create Django view
     with clear comments and minimal dependencies.
284. Copilot task: In the Subdomain Routing phase, Create HTML template
     with clear comments and minimal dependencies.
285. Copilot task: In the Subdomain Routing phase, Create URL route with
     clear comments and minimal dependencies.
286. Copilot task: In the Subdomain Routing phase, Add admin
     registration with clear comments and minimal dependencies.
287. Copilot task: In the Subdomain Routing phase, Write validation
     logic with clear comments and minimal dependencies.
288. Copilot task: In the Subdomain Routing phase, Add Tailwind styling
     with clear comments and minimal dependencies.
289. Copilot task: In the Subdomain Routing phase, Write helper function
     with clear comments and minimal dependencies.
290. Copilot task: In the Subdomain Routing phase, Write unit test with
     clear comments and minimal dependencies.
291. Copilot task: In the Onboarding Wizard phase, Create Django model
     with clear comments and minimal dependencies.
292. Copilot task: In the Onboarding Wizard phase, Create Django form
     with clear comments and minimal dependencies.
293. Copilot task: In the Onboarding Wizard phase, Create Django view
     with clear comments and minimal dependencies.
294. Copilot task: In the Onboarding Wizard phase, Create HTML template
     with clear comments and minimal dependencies.
295. Copilot task: In the Onboarding Wizard phase, Create URL route with
     clear comments and minimal dependencies.
296. Copilot task: In the Onboarding Wizard phase, Add admin
     registration with clear comments and minimal dependencies.
297. Copilot task: In the Onboarding Wizard phase, Write validation
     logic with clear comments and minimal dependencies.
298. Copilot task: In the Onboarding Wizard phase, Add Tailwind styling
     with clear comments and minimal dependencies.
299. Copilot task: In the Onboarding Wizard phase, Write helper function
     with clear comments and minimal dependencies.
300. Copilot task: In the Onboarding Wizard phase, Write unit test with
     clear comments and minimal dependencies.
301. Copilot task: In the Template Library phase, Create Django model
     with clear comments and minimal dependencies.
302. Copilot task: In the Template Library phase, Create Django form
     with clear comments and minimal dependencies.
303. Copilot task: In the Template Library phase, Create Django view
     with clear comments and minimal dependencies.
304. Copilot task: In the Template Library phase, Create HTML template
     with clear comments and minimal dependencies.
305. Copilot task: In the Template Library phase, Create URL route with
     clear comments and minimal dependencies.
306. Copilot task: In the Template Library phase, Add admin registration
     with clear comments and minimal dependencies.
307. Copilot task: In the Template Library phase, Write validation logic
     with clear comments and minimal dependencies.
308. Copilot task: In the Template Library phase, Add Tailwind styling
     with clear comments and minimal dependencies.
309. Copilot task: In the Template Library phase, Write helper function
     with clear comments and minimal dependencies.
310. Copilot task: In the Template Library phase, Write unit test with
     clear comments and minimal dependencies.
311. Copilot task: In the Media Handling phase, Create Django model with
     clear comments and minimal dependencies.
312. Copilot task: In the Media Handling phase, Create Django form with
     clear comments and minimal dependencies.
313. Copilot task: In the Media Handling phase, Create Django view with
     clear comments and minimal dependencies.
314. Copilot task: In the Media Handling phase, Create HTML template
     with clear comments and minimal dependencies.
315. Copilot task: In the Media Handling phase, Create URL route with
     clear comments and minimal dependencies.
316. Copilot task: In the Media Handling phase, Add admin registration
     with clear comments and minimal dependencies.
317. Copilot task: In the Media Handling phase, Write validation logic
     with clear comments and minimal dependencies.
318. Copilot task: In the Media Handling phase, Add Tailwind styling
     with clear comments and minimal dependencies.
319. Copilot task: In the Media Handling phase, Write helper function
     with clear comments and minimal dependencies.
320. Copilot task: In the Media Handling phase, Write unit test with
     clear comments and minimal dependencies.
321. Copilot task: In the Domain Management phase, Create Django model
     with clear comments and minimal dependencies.
322. Copilot task: In the Domain Management phase, Create Django form
     with clear comments and minimal dependencies.
323. Copilot task: In the Domain Management phase, Create Django view
     with clear comments and minimal dependencies.
324. Copilot task: In the Domain Management phase, Create HTML template
     with clear comments and minimal dependencies.
325. Copilot task: In the Domain Management phase, Create URL route with
     clear comments and minimal dependencies.
326. Copilot task: In the Domain Management phase, Add admin
     registration with clear comments and minimal dependencies.
327. Copilot task: In the Domain Management phase, Write validation
     logic with clear comments and minimal dependencies.
328. Copilot task: In the Domain Management phase, Add Tailwind styling
     with clear comments and minimal dependencies.
329. Copilot task: In the Domain Management phase, Write helper function
     with clear comments and minimal dependencies.
330. Copilot task: In the Domain Management phase, Write unit test with
     clear comments and minimal dependencies.
331. Copilot task: In the Admin Tools phase, Create Django model with
     clear comments and minimal dependencies.
332. Copilot task: In the Admin Tools phase, Create Django form with
     clear comments and minimal dependencies.
333. Copilot task: In the Admin Tools phase, Create Django view with
     clear comments and minimal dependencies.
334. Copilot task: In the Admin Tools phase, Create HTML template with
     clear comments and minimal dependencies.
335. Copilot task: In the Admin Tools phase, Create URL route with clear
     comments and minimal dependencies.
336. Copilot task: In the Admin Tools phase, Add admin registration with
     clear comments and minimal dependencies.
337. Copilot task: In the Admin Tools phase, Write validation logic with
     clear comments and minimal dependencies.
338. Copilot task: In the Admin Tools phase, Add Tailwind styling with
     clear comments and minimal dependencies.
339. Copilot task: In the Admin Tools phase, Write helper function with
     clear comments and minimal dependencies.
340. Copilot task: In the Admin Tools phase, Write unit test with clear
     comments and minimal dependencies.
341. Copilot task: In the Testing phase, Create Django model with clear
     comments and minimal dependencies.
342. Copilot task: In the Testing phase, Create Django form with clear
     comments and minimal dependencies.
343. Copilot task: In the Testing phase, Create Django view with clear
     comments and minimal dependencies.
344. Copilot task: In the Testing phase, Create HTML template with clear
     comments and minimal dependencies.
345. Copilot task: In the Testing phase, Create URL route with clear
     comments and minimal dependencies.
346. Copilot task: In the Testing phase, Add admin registration with
     clear comments and minimal dependencies.
347. Copilot task: In the Testing phase, Write validation logic with
     clear comments and minimal dependencies.
348. Copilot task: In the Testing phase, Add Tailwind styling with clear
     comments and minimal dependencies.
349. Copilot task: In the Testing phase, Write helper function with
     clear comments and minimal dependencies.
350. Copilot task: In the Testing phase, Write unit test with clear
     comments and minimal dependencies.
351. Copilot task: In the Deployment Preparation phase, Create Django
     model with clear comments and minimal dependencies.
352. Copilot task: In the Deployment Preparation phase, Create Django
     form with clear comments and minimal dependencies.
353. Copilot task: In the Deployment Preparation phase, Create Django
     view with clear comments and minimal dependencies.
354. Copilot task: In the Deployment Preparation phase, Create HTML
     template with clear comments and minimal dependencies.
355. Copilot task: In the Deployment Preparation phase, Create URL route
     with clear comments and minimal dependencies.
356. Copilot task: In the Deployment Preparation phase, Add admin
     registration with clear comments and minimal dependencies.
357. Copilot task: In the Deployment Preparation phase, Write validation
     logic with clear comments and minimal dependencies.
358. Copilot task: In the Deployment Preparation phase, Add Tailwind
     styling with clear comments and minimal dependencies.
359. Copilot task: In the Deployment Preparation phase, Write helper
     function with clear comments and minimal dependencies.
360. Copilot task: In the Deployment Preparation phase, Write unit test
     with clear comments and minimal dependencies.
361. Copilot task: In the Project Setup phase, Create Django model with
     clear comments and minimal dependencies.
362. Copilot task: In the Project Setup phase, Create Django form with
     clear comments and minimal dependencies.
363. Copilot task: In the Project Setup phase, Create Django view with
     clear comments and minimal dependencies.
364. Copilot task: In the Project Setup phase, Create HTML template with
     clear comments and minimal dependencies.
365. Copilot task: In the Project Setup phase, Create URL route with
     clear comments and minimal dependencies.
366. Copilot task: In the Project Setup phase, Add admin registration
     with clear comments and minimal dependencies.
367. Copilot task: In the Project Setup phase, Write validation logic
     with clear comments and minimal dependencies.
368. Copilot task: In the Project Setup phase, Add Tailwind styling with
     clear comments and minimal dependencies.
369. Copilot task: In the Project Setup phase, Write helper function
     with clear comments and minimal dependencies.
370. Copilot task: In the Project Setup phase, Write unit test with
     clear comments and minimal dependencies.
371. Copilot task: In the Django Configuration phase, Create Django
     model with clear comments and minimal dependencies.
372. Copilot task: In the Django Configuration phase, Create Django form
     with clear comments and minimal dependencies.
373. Copilot task: In the Django Configuration phase, Create Django view
     with clear comments and minimal dependencies.
374. Copilot task: In the Django Configuration phase, Create HTML
     template with clear comments and minimal dependencies.
375. Copilot task: In the Django Configuration phase, Create URL route
     with clear comments and minimal dependencies.
376. Copilot task: In the Django Configuration phase, Add admin
     registration with clear comments and minimal dependencies.
377. Copilot task: In the Django Configuration phase, Write validation
     logic with clear comments and minimal dependencies.
378. Copilot task: In the Django Configuration phase, Add Tailwind
     styling with clear comments and minimal dependencies.
379. Copilot task: In the Django Configuration phase, Write helper
     function with clear comments and minimal dependencies.
380. Copilot task: In the Django Configuration phase, Write unit test
     with clear comments and minimal dependencies.
381. Copilot task: In the Marketing Website phase, Create Django model
     with clear comments and minimal dependencies.
382. Copilot task: In the Marketing Website phase, Create Django form
     with clear comments and minimal dependencies.
383. Copilot task: In the Marketing Website phase, Create Django view
     with clear comments and minimal dependencies.
384. Copilot task: In the Marketing Website phase, Create HTML template
     with clear comments and minimal dependencies.
385. Copilot task: In the Marketing Website phase, Create URL route with
     clear comments and minimal dependencies.
386. Copilot task: In the Marketing Website phase, Add admin
     registration with clear comments and minimal dependencies.
387. Copilot task: In the Marketing Website phase, Write validation
     logic with clear comments and minimal dependencies.
388. Copilot task: In the Marketing Website phase, Add Tailwind styling
     with clear comments and minimal dependencies.
389. Copilot task: In the Marketing Website phase, Write helper function
     with clear comments and minimal dependencies.
390. Copilot task: In the Marketing Website phase, Write unit test with
     clear comments and minimal dependencies.
391. Copilot task: In the Authentication System phase, Create Django
     model with clear comments and minimal dependencies.
392. Copilot task: In the Authentication System phase, Create Django
     form with clear comments and minimal dependencies.
393. Copilot task: In the Authentication System phase, Create Django
     view with clear comments and minimal dependencies.
394. Copilot task: In the Authentication System phase, Create HTML
     template with clear comments and minimal dependencies.
395. Copilot task: In the Authentication System phase, Create URL route
     with clear comments and minimal dependencies.
396. Copilot task: In the Authentication System phase, Add admin
     registration with clear comments and minimal dependencies.
397. Copilot task: In the Authentication System phase, Write validation
     logic with clear comments and minimal dependencies.
398. Copilot task: In the Authentication System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
399. Copilot task: In the Authentication System phase, Write helper
     function with clear comments and minimal dependencies.
400. Copilot task: In the Authentication System phase, Write unit test
     with clear comments and minimal dependencies.
401. Copilot task: In the Dashboard UI phase, Create Django model with
     clear comments and minimal dependencies.
402. Copilot task: In the Dashboard UI phase, Create Django form with
     clear comments and minimal dependencies.
403. Copilot task: In the Dashboard UI phase, Create Django view with
     clear comments and minimal dependencies.
404. Copilot task: In the Dashboard UI phase, Create HTML template with
     clear comments and minimal dependencies.
405. Copilot task: In the Dashboard UI phase, Create URL route with
     clear comments and minimal dependencies.
406. Copilot task: In the Dashboard UI phase, Add admin registration
     with clear comments and minimal dependencies.
407. Copilot task: In the Dashboard UI phase, Write validation logic
     with clear comments and minimal dependencies.
408. Copilot task: In the Dashboard UI phase, Add Tailwind styling with
     clear comments and minimal dependencies.
409. Copilot task: In the Dashboard UI phase, Write helper function with
     clear comments and minimal dependencies.
410. Copilot task: In the Dashboard UI phase, Write unit test with clear
     comments and minimal dependencies.
411. Copilot task: In the CMS Models phase, Create Django model with
     clear comments and minimal dependencies.
412. Copilot task: In the CMS Models phase, Create Django form with
     clear comments and minimal dependencies.
413. Copilot task: In the CMS Models phase, Create Django view with
     clear comments and minimal dependencies.
414. Copilot task: In the CMS Models phase, Create HTML template with
     clear comments and minimal dependencies.
415. Copilot task: In the CMS Models phase, Create URL route with clear
     comments and minimal dependencies.
416. Copilot task: In the CMS Models phase, Add admin registration with
     clear comments and minimal dependencies.
417. Copilot task: In the CMS Models phase, Write validation logic with
     clear comments and minimal dependencies.
418. Copilot task: In the CMS Models phase, Add Tailwind styling with
     clear comments and minimal dependencies.
419. Copilot task: In the CMS Models phase, Write helper function with
     clear comments and minimal dependencies.
420. Copilot task: In the CMS Models phase, Write unit test with clear
     comments and minimal dependencies.
421. Copilot task: In the CMS Forms phase, Create Django model with
     clear comments and minimal dependencies.
422. Copilot task: In the CMS Forms phase, Create Django form with clear
     comments and minimal dependencies.
423. Copilot task: In the CMS Forms phase, Create Django view with clear
     comments and minimal dependencies.
424. Copilot task: In the CMS Forms phase, Create HTML template with
     clear comments and minimal dependencies.
425. Copilot task: In the CMS Forms phase, Create URL route with clear
     comments and minimal dependencies.
426. Copilot task: In the CMS Forms phase, Add admin registration with
     clear comments and minimal dependencies.
427. Copilot task: In the CMS Forms phase, Write validation logic with
     clear comments and minimal dependencies.
428. Copilot task: In the CMS Forms phase, Add Tailwind styling with
     clear comments and minimal dependencies.
429. Copilot task: In the CMS Forms phase, Write helper function with
     clear comments and minimal dependencies.
430. Copilot task: In the CMS Forms phase, Write unit test with clear
     comments and minimal dependencies.
431. Copilot task: In the CMS Views phase, Create Django model with
     clear comments and minimal dependencies.
432. Copilot task: In the CMS Views phase, Create Django form with clear
     comments and minimal dependencies.
433. Copilot task: In the CMS Views phase, Create Django view with clear
     comments and minimal dependencies.
434. Copilot task: In the CMS Views phase, Create HTML template with
     clear comments and minimal dependencies.
435. Copilot task: In the CMS Views phase, Create URL route with clear
     comments and minimal dependencies.
436. Copilot task: In the CMS Views phase, Add admin registration with
     clear comments and minimal dependencies.
437. Copilot task: In the CMS Views phase, Write validation logic with
     clear comments and minimal dependencies.
438. Copilot task: In the CMS Views phase, Add Tailwind styling with
     clear comments and minimal dependencies.
439. Copilot task: In the CMS Views phase, Write helper function with
     clear comments and minimal dependencies.
440. Copilot task: In the CMS Views phase, Write unit test with clear
     comments and minimal dependencies.
441. Copilot task: In the Website Instance System phase, Create Django
     model with clear comments and minimal dependencies.
442. Copilot task: In the Website Instance System phase, Create Django
     form with clear comments and minimal dependencies.
443. Copilot task: In the Website Instance System phase, Create Django
     view with clear comments and minimal dependencies.
444. Copilot task: In the Website Instance System phase, Create HTML
     template with clear comments and minimal dependencies.
445. Copilot task: In the Website Instance System phase, Create URL
     route with clear comments and minimal dependencies.
446. Copilot task: In the Website Instance System phase, Add admin
     registration with clear comments and minimal dependencies.
447. Copilot task: In the Website Instance System phase, Write
     validation logic with clear comments and minimal dependencies.
448. Copilot task: In the Website Instance System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
449. Copilot task: In the Website Instance System phase, Write helper
     function with clear comments and minimal dependencies.
450. Copilot task: In the Website Instance System phase, Write unit test
     with clear comments and minimal dependencies.
451. Copilot task: In the Template Rendering phase, Create Django model
     with clear comments and minimal dependencies.
452. Copilot task: In the Template Rendering phase, Create Django form
     with clear comments and minimal dependencies.
453. Copilot task: In the Template Rendering phase, Create Django view
     with clear comments and minimal dependencies.
454. Copilot task: In the Template Rendering phase, Create HTML template
     with clear comments and minimal dependencies.
455. Copilot task: In the Template Rendering phase, Create URL route
     with clear comments and minimal dependencies.
456. Copilot task: In the Template Rendering phase, Add admin
     registration with clear comments and minimal dependencies.
457. Copilot task: In the Template Rendering phase, Write validation
     logic with clear comments and minimal dependencies.
458. Copilot task: In the Template Rendering phase, Add Tailwind styling
     with clear comments and minimal dependencies.
459. Copilot task: In the Template Rendering phase, Write helper
     function with clear comments and minimal dependencies.
460. Copilot task: In the Template Rendering phase, Write unit test with
     clear comments and minimal dependencies.
461. Copilot task: In the Subdomain Routing phase, Create Django model
     with clear comments and minimal dependencies.
462. Copilot task: In the Subdomain Routing phase, Create Django form
     with clear comments and minimal dependencies.
463. Copilot task: In the Subdomain Routing phase, Create Django view
     with clear comments and minimal dependencies.
464. Copilot task: In the Subdomain Routing phase, Create HTML template
     with clear comments and minimal dependencies.
465. Copilot task: In the Subdomain Routing phase, Create URL route with
     clear comments and minimal dependencies.
466. Copilot task: In the Subdomain Routing phase, Add admin
     registration with clear comments and minimal dependencies.
467. Copilot task: In the Subdomain Routing phase, Write validation
     logic with clear comments and minimal dependencies.
468. Copilot task: In the Subdomain Routing phase, Add Tailwind styling
     with clear comments and minimal dependencies.
469. Copilot task: In the Subdomain Routing phase, Write helper function
     with clear comments and minimal dependencies.
470. Copilot task: In the Subdomain Routing phase, Write unit test with
     clear comments and minimal dependencies.
471. Copilot task: In the Onboarding Wizard phase, Create Django model
     with clear comments and minimal dependencies.
472. Copilot task: In the Onboarding Wizard phase, Create Django form
     with clear comments and minimal dependencies.
473. Copilot task: In the Onboarding Wizard phase, Create Django view
     with clear comments and minimal dependencies.
474. Copilot task: In the Onboarding Wizard phase, Create HTML template
     with clear comments and minimal dependencies.
475. Copilot task: In the Onboarding Wizard phase, Create URL route with
     clear comments and minimal dependencies.
476. Copilot task: In the Onboarding Wizard phase, Add admin
     registration with clear comments and minimal dependencies.
477. Copilot task: In the Onboarding Wizard phase, Write validation
     logic with clear comments and minimal dependencies.
478. Copilot task: In the Onboarding Wizard phase, Add Tailwind styling
     with clear comments and minimal dependencies.
479. Copilot task: In the Onboarding Wizard phase, Write helper function
     with clear comments and minimal dependencies.
480. Copilot task: In the Onboarding Wizard phase, Write unit test with
     clear comments and minimal dependencies.
481. Copilot task: In the Template Library phase, Create Django model
     with clear comments and minimal dependencies.
482. Copilot task: In the Template Library phase, Create Django form
     with clear comments and minimal dependencies.
483. Copilot task: In the Template Library phase, Create Django view
     with clear comments and minimal dependencies.
484. Copilot task: In the Template Library phase, Create HTML template
     with clear comments and minimal dependencies.
485. Copilot task: In the Template Library phase, Create URL route with
     clear comments and minimal dependencies.
486. Copilot task: In the Template Library phase, Add admin registration
     with clear comments and minimal dependencies.
487. Copilot task: In the Template Library phase, Write validation logic
     with clear comments and minimal dependencies.
488. Copilot task: In the Template Library phase, Add Tailwind styling
     with clear comments and minimal dependencies.
489. Copilot task: In the Template Library phase, Write helper function
     with clear comments and minimal dependencies.
490. Copilot task: In the Template Library phase, Write unit test with
     clear comments and minimal dependencies.
491. Copilot task: In the Media Handling phase, Create Django model with
     clear comments and minimal dependencies.
492. Copilot task: In the Media Handling phase, Create Django form with
     clear comments and minimal dependencies.
493. Copilot task: In the Media Handling phase, Create Django view with
     clear comments and minimal dependencies.
494. Copilot task: In the Media Handling phase, Create HTML template
     with clear comments and minimal dependencies.
495. Copilot task: In the Media Handling phase, Create URL route with
     clear comments and minimal dependencies.
496. Copilot task: In the Media Handling phase, Add admin registration
     with clear comments and minimal dependencies.
497. Copilot task: In the Media Handling phase, Write validation logic
     with clear comments and minimal dependencies.
498. Copilot task: In the Media Handling phase, Add Tailwind styling
     with clear comments and minimal dependencies.
499. Copilot task: In the Media Handling phase, Write helper function
     with clear comments and minimal dependencies.
500. Copilot task: In the Media Handling phase, Write unit test with
     clear comments and minimal dependencies.
501. Copilot task: In the Domain Management phase, Create Django model
     with clear comments and minimal dependencies.
502. Copilot task: In the Domain Management phase, Create Django form
     with clear comments and minimal dependencies.
503. Copilot task: In the Domain Management phase, Create Django view
     with clear comments and minimal dependencies.
504. Copilot task: In the Domain Management phase, Create HTML template
     with clear comments and minimal dependencies.
505. Copilot task: In the Domain Management phase, Create URL route with
     clear comments and minimal dependencies.
506. Copilot task: In the Domain Management phase, Add admin
     registration with clear comments and minimal dependencies.
507. Copilot task: In the Domain Management phase, Write validation
     logic with clear comments and minimal dependencies.
508. Copilot task: In the Domain Management phase, Add Tailwind styling
     with clear comments and minimal dependencies.
509. Copilot task: In the Domain Management phase, Write helper function
     with clear comments and minimal dependencies.
510. Copilot task: In the Domain Management phase, Write unit test with
     clear comments and minimal dependencies.
511. Copilot task: In the Admin Tools phase, Create Django model with
     clear comments and minimal dependencies.
512. Copilot task: In the Admin Tools phase, Create Django form with
     clear comments and minimal dependencies.
513. Copilot task: In the Admin Tools phase, Create Django view with
     clear comments and minimal dependencies.
514. Copilot task: In the Admin Tools phase, Create HTML template with
     clear comments and minimal dependencies.
515. Copilot task: In the Admin Tools phase, Create URL route with clear
     comments and minimal dependencies.
516. Copilot task: In the Admin Tools phase, Add admin registration with
     clear comments and minimal dependencies.
517. Copilot task: In the Admin Tools phase, Write validation logic with
     clear comments and minimal dependencies.
518. Copilot task: In the Admin Tools phase, Add Tailwind styling with
     clear comments and minimal dependencies.
519. Copilot task: In the Admin Tools phase, Write helper function with
     clear comments and minimal dependencies.
520. Copilot task: In the Admin Tools phase, Write unit test with clear
     comments and minimal dependencies.
521. Copilot task: In the Testing phase, Create Django model with clear
     comments and minimal dependencies.
522. Copilot task: In the Testing phase, Create Django form with clear
     comments and minimal dependencies.
523. Copilot task: In the Testing phase, Create Django view with clear
     comments and minimal dependencies.
524. Copilot task: In the Testing phase, Create HTML template with clear
     comments and minimal dependencies.
525. Copilot task: In the Testing phase, Create URL route with clear
     comments and minimal dependencies.
526. Copilot task: In the Testing phase, Add admin registration with
     clear comments and minimal dependencies.
527. Copilot task: In the Testing phase, Write validation logic with
     clear comments and minimal dependencies.
528. Copilot task: In the Testing phase, Add Tailwind styling with clear
     comments and minimal dependencies.
529. Copilot task: In the Testing phase, Write helper function with
     clear comments and minimal dependencies.
530. Copilot task: In the Testing phase, Write unit test with clear
     comments and minimal dependencies.
531. Copilot task: In the Deployment Preparation phase, Create Django
     model with clear comments and minimal dependencies.
532. Copilot task: In the Deployment Preparation phase, Create Django
     form with clear comments and minimal dependencies.
533. Copilot task: In the Deployment Preparation phase, Create Django
     view with clear comments and minimal dependencies.
534. Copilot task: In the Deployment Preparation phase, Create HTML
     template with clear comments and minimal dependencies.
535. Copilot task: In the Deployment Preparation phase, Create URL route
     with clear comments and minimal dependencies.
536. Copilot task: In the Deployment Preparation phase, Add admin
     registration with clear comments and minimal dependencies.
537. Copilot task: In the Deployment Preparation phase, Write validation
     logic with clear comments and minimal dependencies.
538. Copilot task: In the Deployment Preparation phase, Add Tailwind
     styling with clear comments and minimal dependencies.
539. Copilot task: In the Deployment Preparation phase, Write helper
     function with clear comments and minimal dependencies.
540. Copilot task: In the Deployment Preparation phase, Write unit test
     with clear comments and minimal dependencies.
541. Copilot task: In the Project Setup phase, Create Django model with
     clear comments and minimal dependencies.
542. Copilot task: In the Project Setup phase, Create Django form with
     clear comments and minimal dependencies.
543. Copilot task: In the Project Setup phase, Create Django view with
     clear comments and minimal dependencies.
544. Copilot task: In the Project Setup phase, Create HTML template with
     clear comments and minimal dependencies.
545. Copilot task: In the Project Setup phase, Create URL route with
     clear comments and minimal dependencies.
546. Copilot task: In the Project Setup phase, Add admin registration
     with clear comments and minimal dependencies.
547. Copilot task: In the Project Setup phase, Write validation logic
     with clear comments and minimal dependencies.
548. Copilot task: In the Project Setup phase, Add Tailwind styling with
     clear comments and minimal dependencies.
549. Copilot task: In the Project Setup phase, Write helper function
     with clear comments and minimal dependencies.
550. Copilot task: In the Project Setup phase, Write unit test with
     clear comments and minimal dependencies.
551. Copilot task: In the Django Configuration phase, Create Django
     model with clear comments and minimal dependencies.
552. Copilot task: In the Django Configuration phase, Create Django form
     with clear comments and minimal dependencies.
553. Copilot task: In the Django Configuration phase, Create Django view
     with clear comments and minimal dependencies.
554. Copilot task: In the Django Configuration phase, Create HTML
     template with clear comments and minimal dependencies.
555. Copilot task: In the Django Configuration phase, Create URL route
     with clear comments and minimal dependencies.
556. Copilot task: In the Django Configuration phase, Add admin
     registration with clear comments and minimal dependencies.
557. Copilot task: In the Django Configuration phase, Write validation
     logic with clear comments and minimal dependencies.
558. Copilot task: In the Django Configuration phase, Add Tailwind
     styling with clear comments and minimal dependencies.
559. Copilot task: In the Django Configuration phase, Write helper
     function with clear comments and minimal dependencies.
560. Copilot task: In the Django Configuration phase, Write unit test
     with clear comments and minimal dependencies.
561. Copilot task: In the Marketing Website phase, Create Django model
     with clear comments and minimal dependencies.
562. Copilot task: In the Marketing Website phase, Create Django form
     with clear comments and minimal dependencies.
563. Copilot task: In the Marketing Website phase, Create Django view
     with clear comments and minimal dependencies.
564. Copilot task: In the Marketing Website phase, Create HTML template
     with clear comments and minimal dependencies.
565. Copilot task: In the Marketing Website phase, Create URL route with
     clear comments and minimal dependencies.
566. Copilot task: In the Marketing Website phase, Add admin
     registration with clear comments and minimal dependencies.
567. Copilot task: In the Marketing Website phase, Write validation
     logic with clear comments and minimal dependencies.
568. Copilot task: In the Marketing Website phase, Add Tailwind styling
     with clear comments and minimal dependencies.
569. Copilot task: In the Marketing Website phase, Write helper function
     with clear comments and minimal dependencies.
570. Copilot task: In the Marketing Website phase, Write unit test with
     clear comments and minimal dependencies.
571. Copilot task: In the Authentication System phase, Create Django
     model with clear comments and minimal dependencies.
572. Copilot task: In the Authentication System phase, Create Django
     form with clear comments and minimal dependencies.
573. Copilot task: In the Authentication System phase, Create Django
     view with clear comments and minimal dependencies.
574. Copilot task: In the Authentication System phase, Create HTML
     template with clear comments and minimal dependencies.
575. Copilot task: In the Authentication System phase, Create URL route
     with clear comments and minimal dependencies.
576. Copilot task: In the Authentication System phase, Add admin
     registration with clear comments and minimal dependencies.
577. Copilot task: In the Authentication System phase, Write validation
     logic with clear comments and minimal dependencies.
578. Copilot task: In the Authentication System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
579. Copilot task: In the Authentication System phase, Write helper
     function with clear comments and minimal dependencies.
580. Copilot task: In the Authentication System phase, Write unit test
     with clear comments and minimal dependencies.
581. Copilot task: In the Dashboard UI phase, Create Django model with
     clear comments and minimal dependencies.
582. Copilot task: In the Dashboard UI phase, Create Django form with
     clear comments and minimal dependencies.
583. Copilot task: In the Dashboard UI phase, Create Django view with
     clear comments and minimal dependencies.
584. Copilot task: In the Dashboard UI phase, Create HTML template with
     clear comments and minimal dependencies.
585. Copilot task: In the Dashboard UI phase, Create URL route with
     clear comments and minimal dependencies.
586. Copilot task: In the Dashboard UI phase, Add admin registration
     with clear comments and minimal dependencies.
587. Copilot task: In the Dashboard UI phase, Write validation logic
     with clear comments and minimal dependencies.
588. Copilot task: In the Dashboard UI phase, Add Tailwind styling with
     clear comments and minimal dependencies.
589. Copilot task: In the Dashboard UI phase, Write helper function with
     clear comments and minimal dependencies.
590. Copilot task: In the Dashboard UI phase, Write unit test with clear
     comments and minimal dependencies.
591. Copilot task: In the CMS Models phase, Create Django model with
     clear comments and minimal dependencies.
592. Copilot task: In the CMS Models phase, Create Django form with
     clear comments and minimal dependencies.
593. Copilot task: In the CMS Models phase, Create Django view with
     clear comments and minimal dependencies.
594. Copilot task: In the CMS Models phase, Create HTML template with
     clear comments and minimal dependencies.
595. Copilot task: In the CMS Models phase, Create URL route with clear
     comments and minimal dependencies.
596. Copilot task: In the CMS Models phase, Add admin registration with
     clear comments and minimal dependencies.
597. Copilot task: In the CMS Models phase, Write validation logic with
     clear comments and minimal dependencies.
598. Copilot task: In the CMS Models phase, Add Tailwind styling with
     clear comments and minimal dependencies.
599. Copilot task: In the CMS Models phase, Write helper function with
     clear comments and minimal dependencies.
600. Copilot task: In the CMS Models phase, Write unit test with clear
     comments and minimal dependencies.
601. Copilot task: In the CMS Forms phase, Create Django model with
     clear comments and minimal dependencies.
602. Copilot task: In the CMS Forms phase, Create Django form with clear
     comments and minimal dependencies.
603. Copilot task: In the CMS Forms phase, Create Django view with clear
     comments and minimal dependencies.
604. Copilot task: In the CMS Forms phase, Create HTML template with
     clear comments and minimal dependencies.
605. Copilot task: In the CMS Forms phase, Create URL route with clear
     comments and minimal dependencies.
606. Copilot task: In the CMS Forms phase, Add admin registration with
     clear comments and minimal dependencies.
607. Copilot task: In the CMS Forms phase, Write validation logic with
     clear comments and minimal dependencies.
608. Copilot task: In the CMS Forms phase, Add Tailwind styling with
     clear comments and minimal dependencies.
609. Copilot task: In the CMS Forms phase, Write helper function with
     clear comments and minimal dependencies.
610. Copilot task: In the CMS Forms phase, Write unit test with clear
     comments and minimal dependencies.
611. Copilot task: In the CMS Views phase, Create Django model with
     clear comments and minimal dependencies.
612. Copilot task: In the CMS Views phase, Create Django form with clear
     comments and minimal dependencies.
613. Copilot task: In the CMS Views phase, Create Django view with clear
     comments and minimal dependencies.
614. Copilot task: In the CMS Views phase, Create HTML template with
     clear comments and minimal dependencies.
615. Copilot task: In the CMS Views phase, Create URL route with clear
     comments and minimal dependencies.
616. Copilot task: In the CMS Views phase, Add admin registration with
     clear comments and minimal dependencies.
617. Copilot task: In the CMS Views phase, Write validation logic with
     clear comments and minimal dependencies.
618. Copilot task: In the CMS Views phase, Add Tailwind styling with
     clear comments and minimal dependencies.
619. Copilot task: In the CMS Views phase, Write helper function with
     clear comments and minimal dependencies.
620. Copilot task: In the CMS Views phase, Write unit test with clear
     comments and minimal dependencies.
621. Copilot task: In the Website Instance System phase, Create Django
     model with clear comments and minimal dependencies.
622. Copilot task: In the Website Instance System phase, Create Django
     form with clear comments and minimal dependencies.
623. Copilot task: In the Website Instance System phase, Create Django
     view with clear comments and minimal dependencies.
624. Copilot task: In the Website Instance System phase, Create HTML
     template with clear comments and minimal dependencies.
625. Copilot task: In the Website Instance System phase, Create URL
     route with clear comments and minimal dependencies.
626. Copilot task: In the Website Instance System phase, Add admin
     registration with clear comments and minimal dependencies.
627. Copilot task: In the Website Instance System phase, Write
     validation logic with clear comments and minimal dependencies.
628. Copilot task: In the Website Instance System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
629. Copilot task: In the Website Instance System phase, Write helper
     function with clear comments and minimal dependencies.
630. Copilot task: In the Website Instance System phase, Write unit test
     with clear comments and minimal dependencies.
631. Copilot task: In the Template Rendering phase, Create Django model
     with clear comments and minimal dependencies.
632. Copilot task: In the Template Rendering phase, Create Django form
     with clear comments and minimal dependencies.
633. Copilot task: In the Template Rendering phase, Create Django view
     with clear comments and minimal dependencies.
634. Copilot task: In the Template Rendering phase, Create HTML template
     with clear comments and minimal dependencies.
635. Copilot task: In the Template Rendering phase, Create URL route
     with clear comments and minimal dependencies.
636. Copilot task: In the Template Rendering phase, Add admin
     registration with clear comments and minimal dependencies.
637. Copilot task: In the Template Rendering phase, Write validation
     logic with clear comments and minimal dependencies.
638. Copilot task: In the Template Rendering phase, Add Tailwind styling
     with clear comments and minimal dependencies.
639. Copilot task: In the Template Rendering phase, Write helper
     function with clear comments and minimal dependencies.
640. Copilot task: In the Template Rendering phase, Write unit test with
     clear comments and minimal dependencies.
641. Copilot task: In the Subdomain Routing phase, Create Django model
     with clear comments and minimal dependencies.
642. Copilot task: In the Subdomain Routing phase, Create Django form
     with clear comments and minimal dependencies.
643. Copilot task: In the Subdomain Routing phase, Create Django view
     with clear comments and minimal dependencies.
644. Copilot task: In the Subdomain Routing phase, Create HTML template
     with clear comments and minimal dependencies.
645. Copilot task: In the Subdomain Routing phase, Create URL route with
     clear comments and minimal dependencies.
646. Copilot task: In the Subdomain Routing phase, Add admin
     registration with clear comments and minimal dependencies.
647. Copilot task: In the Subdomain Routing phase, Write validation
     logic with clear comments and minimal dependencies.
648. Copilot task: In the Subdomain Routing phase, Add Tailwind styling
     with clear comments and minimal dependencies.
649. Copilot task: In the Subdomain Routing phase, Write helper function
     with clear comments and minimal dependencies.
650. Copilot task: In the Subdomain Routing phase, Write unit test with
     clear comments and minimal dependencies.
651. Copilot task: In the Onboarding Wizard phase, Create Django model
     with clear comments and minimal dependencies.
652. Copilot task: In the Onboarding Wizard phase, Create Django form
     with clear comments and minimal dependencies.
653. Copilot task: In the Onboarding Wizard phase, Create Django view
     with clear comments and minimal dependencies.
654. Copilot task: In the Onboarding Wizard phase, Create HTML template
     with clear comments and minimal dependencies.
655. Copilot task: In the Onboarding Wizard phase, Create URL route with
     clear comments and minimal dependencies.
656. Copilot task: In the Onboarding Wizard phase, Add admin
     registration with clear comments and minimal dependencies.
657. Copilot task: In the Onboarding Wizard phase, Write validation
     logic with clear comments and minimal dependencies.
658. Copilot task: In the Onboarding Wizard phase, Add Tailwind styling
     with clear comments and minimal dependencies.
659. Copilot task: In the Onboarding Wizard phase, Write helper function
     with clear comments and minimal dependencies.
660. Copilot task: In the Onboarding Wizard phase, Write unit test with
     clear comments and minimal dependencies.
661. Copilot task: In the Template Library phase, Create Django model
     with clear comments and minimal dependencies.
662. Copilot task: In the Template Library phase, Create Django form
     with clear comments and minimal dependencies.
663. Copilot task: In the Template Library phase, Create Django view
     with clear comments and minimal dependencies.
664. Copilot task: In the Template Library phase, Create HTML template
     with clear comments and minimal dependencies.
665. Copilot task: In the Template Library phase, Create URL route with
     clear comments and minimal dependencies.
666. Copilot task: In the Template Library phase, Add admin registration
     with clear comments and minimal dependencies.
667. Copilot task: In the Template Library phase, Write validation logic
     with clear comments and minimal dependencies.
668. Copilot task: In the Template Library phase, Add Tailwind styling
     with clear comments and minimal dependencies.
669. Copilot task: In the Template Library phase, Write helper function
     with clear comments and minimal dependencies.
670. Copilot task: In the Template Library phase, Write unit test with
     clear comments and minimal dependencies.
671. Copilot task: In the Media Handling phase, Create Django model with
     clear comments and minimal dependencies.
672. Copilot task: In the Media Handling phase, Create Django form with
     clear comments and minimal dependencies.
673. Copilot task: In the Media Handling phase, Create Django view with
     clear comments and minimal dependencies.
674. Copilot task: In the Media Handling phase, Create HTML template
     with clear comments and minimal dependencies.
675. Copilot task: In the Media Handling phase, Create URL route with
     clear comments and minimal dependencies.
676. Copilot task: In the Media Handling phase, Add admin registration
     with clear comments and minimal dependencies.
677. Copilot task: In the Media Handling phase, Write validation logic
     with clear comments and minimal dependencies.
678. Copilot task: In the Media Handling phase, Add Tailwind styling
     with clear comments and minimal dependencies.
679. Copilot task: In the Media Handling phase, Write helper function
     with clear comments and minimal dependencies.
680. Copilot task: In the Media Handling phase, Write unit test with
     clear comments and minimal dependencies.
681. Copilot task: In the Domain Management phase, Create Django model
     with clear comments and minimal dependencies.
682. Copilot task: In the Domain Management phase, Create Django form
     with clear comments and minimal dependencies.
683. Copilot task: In the Domain Management phase, Create Django view
     with clear comments and minimal dependencies.
684. Copilot task: In the Domain Management phase, Create HTML template
     with clear comments and minimal dependencies.
685. Copilot task: In the Domain Management phase, Create URL route with
     clear comments and minimal dependencies.
686. Copilot task: In the Domain Management phase, Add admin
     registration with clear comments and minimal dependencies.
687. Copilot task: In the Domain Management phase, Write validation
     logic with clear comments and minimal dependencies.
688. Copilot task: In the Domain Management phase, Add Tailwind styling
     with clear comments and minimal dependencies.
689. Copilot task: In the Domain Management phase, Write helper function
     with clear comments and minimal dependencies.
690. Copilot task: In the Domain Management phase, Write unit test with
     clear comments and minimal dependencies.
691. Copilot task: In the Admin Tools phase, Create Django model with
     clear comments and minimal dependencies.
692. Copilot task: In the Admin Tools phase, Create Django form with
     clear comments and minimal dependencies.
693. Copilot task: In the Admin Tools phase, Create Django view with
     clear comments and minimal dependencies.
694. Copilot task: In the Admin Tools phase, Create HTML template with
     clear comments and minimal dependencies.
695. Copilot task: In the Admin Tools phase, Create URL route with clear
     comments and minimal dependencies.
696. Copilot task: In the Admin Tools phase, Add admin registration with
     clear comments and minimal dependencies.
697. Copilot task: In the Admin Tools phase, Write validation logic with
     clear comments and minimal dependencies.
698. Copilot task: In the Admin Tools phase, Add Tailwind styling with
     clear comments and minimal dependencies.
699. Copilot task: In the Admin Tools phase, Write helper function with
     clear comments and minimal dependencies.
700. Copilot task: In the Admin Tools phase, Write unit test with clear
     comments and minimal dependencies.
701. Copilot task: In the Testing phase, Create Django model with clear
     comments and minimal dependencies.
702. Copilot task: In the Testing phase, Create Django form with clear
     comments and minimal dependencies.
703. Copilot task: In the Testing phase, Create Django view with clear
     comments and minimal dependencies.
704. Copilot task: In the Testing phase, Create HTML template with clear
     comments and minimal dependencies.
705. Copilot task: In the Testing phase, Create URL route with clear
     comments and minimal dependencies.
706. Copilot task: In the Testing phase, Add admin registration with
     clear comments and minimal dependencies.
707. Copilot task: In the Testing phase, Write validation logic with
     clear comments and minimal dependencies.
708. Copilot task: In the Testing phase, Add Tailwind styling with clear
     comments and minimal dependencies.
709. Copilot task: In the Testing phase, Write helper function with
     clear comments and minimal dependencies.
710. Copilot task: In the Testing phase, Write unit test with clear
     comments and minimal dependencies.
711. Copilot task: In the Deployment Preparation phase, Create Django
     model with clear comments and minimal dependencies.
712. Copilot task: In the Deployment Preparation phase, Create Django
     form with clear comments and minimal dependencies.
713. Copilot task: In the Deployment Preparation phase, Create Django
     view with clear comments and minimal dependencies.
714. Copilot task: In the Deployment Preparation phase, Create HTML
     template with clear comments and minimal dependencies.
715. Copilot task: In the Deployment Preparation phase, Create URL route
     with clear comments and minimal dependencies.
716. Copilot task: In the Deployment Preparation phase, Add admin
     registration with clear comments and minimal dependencies.
717. Copilot task: In the Deployment Preparation phase, Write validation
     logic with clear comments and minimal dependencies.
718. Copilot task: In the Deployment Preparation phase, Add Tailwind
     styling with clear comments and minimal dependencies.
719. Copilot task: In the Deployment Preparation phase, Write helper
     function with clear comments and minimal dependencies.
720. Copilot task: In the Deployment Preparation phase, Write unit test
     with clear comments and minimal dependencies.
721. Copilot task: In the Project Setup phase, Create Django model with
     clear comments and minimal dependencies.
722. Copilot task: In the Project Setup phase, Create Django form with
     clear comments and minimal dependencies.
723. Copilot task: In the Project Setup phase, Create Django view with
     clear comments and minimal dependencies.
724. Copilot task: In the Project Setup phase, Create HTML template with
     clear comments and minimal dependencies.
725. Copilot task: In the Project Setup phase, Create URL route with
     clear comments and minimal dependencies.
726. Copilot task: In the Project Setup phase, Add admin registration
     with clear comments and minimal dependencies.
727. Copilot task: In the Project Setup phase, Write validation logic
     with clear comments and minimal dependencies.
728. Copilot task: In the Project Setup phase, Add Tailwind styling with
     clear comments and minimal dependencies.
729. Copilot task: In the Project Setup phase, Write helper function
     with clear comments and minimal dependencies.
730. Copilot task: In the Project Setup phase, Write unit test with
     clear comments and minimal dependencies.
731. Copilot task: In the Django Configuration phase, Create Django
     model with clear comments and minimal dependencies.
732. Copilot task: In the Django Configuration phase, Create Django form
     with clear comments and minimal dependencies.
733. Copilot task: In the Django Configuration phase, Create Django view
     with clear comments and minimal dependencies.
734. Copilot task: In the Django Configuration phase, Create HTML
     template with clear comments and minimal dependencies.
735. Copilot task: In the Django Configuration phase, Create URL route
     with clear comments and minimal dependencies.
736. Copilot task: In the Django Configuration phase, Add admin
     registration with clear comments and minimal dependencies.
737. Copilot task: In the Django Configuration phase, Write validation
     logic with clear comments and minimal dependencies.
738. Copilot task: In the Django Configuration phase, Add Tailwind
     styling with clear comments and minimal dependencies.
739. Copilot task: In the Django Configuration phase, Write helper
     function with clear comments and minimal dependencies.
740. Copilot task: In the Django Configuration phase, Write unit test
     with clear comments and minimal dependencies.
741. Copilot task: In the Marketing Website phase, Create Django model
     with clear comments and minimal dependencies.
742. Copilot task: In the Marketing Website phase, Create Django form
     with clear comments and minimal dependencies.
743. Copilot task: In the Marketing Website phase, Create Django view
     with clear comments and minimal dependencies.
744. Copilot task: In the Marketing Website phase, Create HTML template
     with clear comments and minimal dependencies.
745. Copilot task: In the Marketing Website phase, Create URL route with
     clear comments and minimal dependencies.
746. Copilot task: In the Marketing Website phase, Add admin
     registration with clear comments and minimal dependencies.
747. Copilot task: In the Marketing Website phase, Write validation
     logic with clear comments and minimal dependencies.
748. Copilot task: In the Marketing Website phase, Add Tailwind styling
     with clear comments and minimal dependencies.
749. Copilot task: In the Marketing Website phase, Write helper function
     with clear comments and minimal dependencies.
750. Copilot task: In the Marketing Website phase, Write unit test with
     clear comments and minimal dependencies.
751. Copilot task: In the Authentication System phase, Create Django
     model with clear comments and minimal dependencies.
752. Copilot task: In the Authentication System phase, Create Django
     form with clear comments and minimal dependencies.
753. Copilot task: In the Authentication System phase, Create Django
     view with clear comments and minimal dependencies.
754. Copilot task: In the Authentication System phase, Create HTML
     template with clear comments and minimal dependencies.
755. Copilot task: In the Authentication System phase, Create URL route
     with clear comments and minimal dependencies.
756. Copilot task: In the Authentication System phase, Add admin
     registration with clear comments and minimal dependencies.
757. Copilot task: In the Authentication System phase, Write validation
     logic with clear comments and minimal dependencies.
758. Copilot task: In the Authentication System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
759. Copilot task: In the Authentication System phase, Write helper
     function with clear comments and minimal dependencies.
760. Copilot task: In the Authentication System phase, Write unit test
     with clear comments and minimal dependencies.
761. Copilot task: In the Dashboard UI phase, Create Django model with
     clear comments and minimal dependencies.
762. Copilot task: In the Dashboard UI phase, Create Django form with
     clear comments and minimal dependencies.
763. Copilot task: In the Dashboard UI phase, Create Django view with
     clear comments and minimal dependencies.
764. Copilot task: In the Dashboard UI phase, Create HTML template with
     clear comments and minimal dependencies.
765. Copilot task: In the Dashboard UI phase, Create URL route with
     clear comments and minimal dependencies.
766. Copilot task: In the Dashboard UI phase, Add admin registration
     with clear comments and minimal dependencies.
767. Copilot task: In the Dashboard UI phase, Write validation logic
     with clear comments and minimal dependencies.
768. Copilot task: In the Dashboard UI phase, Add Tailwind styling with
     clear comments and minimal dependencies.
769. Copilot task: In the Dashboard UI phase, Write helper function with
     clear comments and minimal dependencies.
770. Copilot task: In the Dashboard UI phase, Write unit test with clear
     comments and minimal dependencies.
771. Copilot task: In the CMS Models phase, Create Django model with
     clear comments and minimal dependencies.
772. Copilot task: In the CMS Models phase, Create Django form with
     clear comments and minimal dependencies.
773. Copilot task: In the CMS Models phase, Create Django view with
     clear comments and minimal dependencies.
774. Copilot task: In the CMS Models phase, Create HTML template with
     clear comments and minimal dependencies.
775. Copilot task: In the CMS Models phase, Create URL route with clear
     comments and minimal dependencies.
776. Copilot task: In the CMS Models phase, Add admin registration with
     clear comments and minimal dependencies.
777. Copilot task: In the CMS Models phase, Write validation logic with
     clear comments and minimal dependencies.
778. Copilot task: In the CMS Models phase, Add Tailwind styling with
     clear comments and minimal dependencies.
779. Copilot task: In the CMS Models phase, Write helper function with
     clear comments and minimal dependencies.
780. Copilot task: In the CMS Models phase, Write unit test with clear
     comments and minimal dependencies.
781. Copilot task: In the CMS Forms phase, Create Django model with
     clear comments and minimal dependencies.
782. Copilot task: In the CMS Forms phase, Create Django form with clear
     comments and minimal dependencies.
783. Copilot task: In the CMS Forms phase, Create Django view with clear
     comments and minimal dependencies.
784. Copilot task: In the CMS Forms phase, Create HTML template with
     clear comments and minimal dependencies.
785. Copilot task: In the CMS Forms phase, Create URL route with clear
     comments and minimal dependencies.
786. Copilot task: In the CMS Forms phase, Add admin registration with
     clear comments and minimal dependencies.
787. Copilot task: In the CMS Forms phase, Write validation logic with
     clear comments and minimal dependencies.
788. Copilot task: In the CMS Forms phase, Add Tailwind styling with
     clear comments and minimal dependencies.
789. Copilot task: In the CMS Forms phase, Write helper function with
     clear comments and minimal dependencies.
790. Copilot task: In the CMS Forms phase, Write unit test with clear
     comments and minimal dependencies.
791. Copilot task: In the CMS Views phase, Create Django model with
     clear comments and minimal dependencies.
792. Copilot task: In the CMS Views phase, Create Django form with clear
     comments and minimal dependencies.
793. Copilot task: In the CMS Views phase, Create Django view with clear
     comments and minimal dependencies.
794. Copilot task: In the CMS Views phase, Create HTML template with
     clear comments and minimal dependencies.
795. Copilot task: In the CMS Views phase, Create URL route with clear
     comments and minimal dependencies.
796. Copilot task: In the CMS Views phase, Add admin registration with
     clear comments and minimal dependencies.
797. Copilot task: In the CMS Views phase, Write validation logic with
     clear comments and minimal dependencies.
798. Copilot task: In the CMS Views phase, Add Tailwind styling with
     clear comments and minimal dependencies.
799. Copilot task: In the CMS Views phase, Write helper function with
     clear comments and minimal dependencies.
800. Copilot task: In the CMS Views phase, Write unit test with clear
     comments and minimal dependencies.
801. Copilot task: In the Website Instance System phase, Create Django
     model with clear comments and minimal dependencies.
802. Copilot task: In the Website Instance System phase, Create Django
     form with clear comments and minimal dependencies.
803. Copilot task: In the Website Instance System phase, Create Django
     view with clear comments and minimal dependencies.
804. Copilot task: In the Website Instance System phase, Create HTML
     template with clear comments and minimal dependencies.
805. Copilot task: In the Website Instance System phase, Create URL
     route with clear comments and minimal dependencies.
806. Copilot task: In the Website Instance System phase, Add admin
     registration with clear comments and minimal dependencies.
807. Copilot task: In the Website Instance System phase, Write
     validation logic with clear comments and minimal dependencies.
808. Copilot task: In the Website Instance System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
809. Copilot task: In the Website Instance System phase, Write helper
     function with clear comments and minimal dependencies.
810. Copilot task: In the Website Instance System phase, Write unit test
     with clear comments and minimal dependencies.
811. Copilot task: In the Template Rendering phase, Create Django model
     with clear comments and minimal dependencies.
812. Copilot task: In the Template Rendering phase, Create Django form
     with clear comments and minimal dependencies.
813. Copilot task: In the Template Rendering phase, Create Django view
     with clear comments and minimal dependencies.
814. Copilot task: In the Template Rendering phase, Create HTML template
     with clear comments and minimal dependencies.
815. Copilot task: In the Template Rendering phase, Create URL route
     with clear comments and minimal dependencies.
816. Copilot task: In the Template Rendering phase, Add admin
     registration with clear comments and minimal dependencies.
817. Copilot task: In the Template Rendering phase, Write validation
     logic with clear comments and minimal dependencies.
818. Copilot task: In the Template Rendering phase, Add Tailwind styling
     with clear comments and minimal dependencies.
819. Copilot task: In the Template Rendering phase, Write helper
     function with clear comments and minimal dependencies.
820. Copilot task: In the Template Rendering phase, Write unit test with
     clear comments and minimal dependencies.
821. Copilot task: In the Subdomain Routing phase, Create Django model
     with clear comments and minimal dependencies.
822. Copilot task: In the Subdomain Routing phase, Create Django form
     with clear comments and minimal dependencies.
823. Copilot task: In the Subdomain Routing phase, Create Django view
     with clear comments and minimal dependencies.
824. Copilot task: In the Subdomain Routing phase, Create HTML template
     with clear comments and minimal dependencies.
825. Copilot task: In the Subdomain Routing phase, Create URL route with
     clear comments and minimal dependencies.
826. Copilot task: In the Subdomain Routing phase, Add admin
     registration with clear comments and minimal dependencies.
827. Copilot task: In the Subdomain Routing phase, Write validation
     logic with clear comments and minimal dependencies.
828. Copilot task: In the Subdomain Routing phase, Add Tailwind styling
     with clear comments and minimal dependencies.
829. Copilot task: In the Subdomain Routing phase, Write helper function
     with clear comments and minimal dependencies.
830. Copilot task: In the Subdomain Routing phase, Write unit test with
     clear comments and minimal dependencies.
831. Copilot task: In the Onboarding Wizard phase, Create Django model
     with clear comments and minimal dependencies.
832. Copilot task: In the Onboarding Wizard phase, Create Django form
     with clear comments and minimal dependencies.
833. Copilot task: In the Onboarding Wizard phase, Create Django view
     with clear comments and minimal dependencies.
834. Copilot task: In the Onboarding Wizard phase, Create HTML template
     with clear comments and minimal dependencies.
835. Copilot task: In the Onboarding Wizard phase, Create URL route with
     clear comments and minimal dependencies.
836. Copilot task: In the Onboarding Wizard phase, Add admin
     registration with clear comments and minimal dependencies.
837. Copilot task: In the Onboarding Wizard phase, Write validation
     logic with clear comments and minimal dependencies.
838. Copilot task: In the Onboarding Wizard phase, Add Tailwind styling
     with clear comments and minimal dependencies.
839. Copilot task: In the Onboarding Wizard phase, Write helper function
     with clear comments and minimal dependencies.
840. Copilot task: In the Onboarding Wizard phase, Write unit test with
     clear comments and minimal dependencies.
841. Copilot task: In the Template Library phase, Create Django model
     with clear comments and minimal dependencies.
842. Copilot task: In the Template Library phase, Create Django form
     with clear comments and minimal dependencies.
843. Copilot task: In the Template Library phase, Create Django view
     with clear comments and minimal dependencies.
844. Copilot task: In the Template Library phase, Create HTML template
     with clear comments and minimal dependencies.
845. Copilot task: In the Template Library phase, Create URL route with
     clear comments and minimal dependencies.
846. Copilot task: In the Template Library phase, Add admin registration
     with clear comments and minimal dependencies.
847. Copilot task: In the Template Library phase, Write validation logic
     with clear comments and minimal dependencies.
848. Copilot task: In the Template Library phase, Add Tailwind styling
     with clear comments and minimal dependencies.
849. Copilot task: In the Template Library phase, Write helper function
     with clear comments and minimal dependencies.
850. Copilot task: In the Template Library phase, Write unit test with
     clear comments and minimal dependencies.
851. Copilot task: In the Media Handling phase, Create Django model with
     clear comments and minimal dependencies.
852. Copilot task: In the Media Handling phase, Create Django form with
     clear comments and minimal dependencies.
853. Copilot task: In the Media Handling phase, Create Django view with
     clear comments and minimal dependencies.
854. Copilot task: In the Media Handling phase, Create HTML template
     with clear comments and minimal dependencies.
855. Copilot task: In the Media Handling phase, Create URL route with
     clear comments and minimal dependencies.
856. Copilot task: In the Media Handling phase, Add admin registration
     with clear comments and minimal dependencies.
857. Copilot task: In the Media Handling phase, Write validation logic
     with clear comments and minimal dependencies.
858. Copilot task: In the Media Handling phase, Add Tailwind styling
     with clear comments and minimal dependencies.
859. Copilot task: In the Media Handling phase, Write helper function
     with clear comments and minimal dependencies.
860. Copilot task: In the Media Handling phase, Write unit test with
     clear comments and minimal dependencies.
861. Copilot task: In the Domain Management phase, Create Django model
     with clear comments and minimal dependencies.
862. Copilot task: In the Domain Management phase, Create Django form
     with clear comments and minimal dependencies.
863. Copilot task: In the Domain Management phase, Create Django view
     with clear comments and minimal dependencies.
864. Copilot task: In the Domain Management phase, Create HTML template
     with clear comments and minimal dependencies.
865. Copilot task: In the Domain Management phase, Create URL route with
     clear comments and minimal dependencies.
866. Copilot task: In the Domain Management phase, Add admin
     registration with clear comments and minimal dependencies.
867. Copilot task: In the Domain Management phase, Write validation
     logic with clear comments and minimal dependencies.
868. Copilot task: In the Domain Management phase, Add Tailwind styling
     with clear comments and minimal dependencies.
869. Copilot task: In the Domain Management phase, Write helper function
     with clear comments and minimal dependencies.
870. Copilot task: In the Domain Management phase, Write unit test with
     clear comments and minimal dependencies.
871. Copilot task: In the Admin Tools phase, Create Django model with
     clear comments and minimal dependencies.
872. Copilot task: In the Admin Tools phase, Create Django form with
     clear comments and minimal dependencies.
873. Copilot task: In the Admin Tools phase, Create Django view with
     clear comments and minimal dependencies.
874. Copilot task: In the Admin Tools phase, Create HTML template with
     clear comments and minimal dependencies.
875. Copilot task: In the Admin Tools phase, Create URL route with clear
     comments and minimal dependencies.
876. Copilot task: In the Admin Tools phase, Add admin registration with
     clear comments and minimal dependencies.
877. Copilot task: In the Admin Tools phase, Write validation logic with
     clear comments and minimal dependencies.
878. Copilot task: In the Admin Tools phase, Add Tailwind styling with
     clear comments and minimal dependencies.
879. Copilot task: In the Admin Tools phase, Write helper function with
     clear comments and minimal dependencies.
880. Copilot task: In the Admin Tools phase, Write unit test with clear
     comments and minimal dependencies.
881. Copilot task: In the Testing phase, Create Django model with clear
     comments and minimal dependencies.
882. Copilot task: In the Testing phase, Create Django form with clear
     comments and minimal dependencies.
883. Copilot task: In the Testing phase, Create Django view with clear
     comments and minimal dependencies.
884. Copilot task: In the Testing phase, Create HTML template with clear
     comments and minimal dependencies.
885. Copilot task: In the Testing phase, Create URL route with clear
     comments and minimal dependencies.
886. Copilot task: In the Testing phase, Add admin registration with
     clear comments and minimal dependencies.
887. Copilot task: In the Testing phase, Write validation logic with
     clear comments and minimal dependencies.
888. Copilot task: In the Testing phase, Add Tailwind styling with clear
     comments and minimal dependencies.
889. Copilot task: In the Testing phase, Write helper function with
     clear comments and minimal dependencies.
890. Copilot task: In the Testing phase, Write unit test with clear
     comments and minimal dependencies.
891. Copilot task: In the Deployment Preparation phase, Create Django
     model with clear comments and minimal dependencies.
892. Copilot task: In the Deployment Preparation phase, Create Django
     form with clear comments and minimal dependencies.
893. Copilot task: In the Deployment Preparation phase, Create Django
     view with clear comments and minimal dependencies.
894. Copilot task: In the Deployment Preparation phase, Create HTML
     template with clear comments and minimal dependencies.
895. Copilot task: In the Deployment Preparation phase, Create URL route
     with clear comments and minimal dependencies.
896. Copilot task: In the Deployment Preparation phase, Add admin
     registration with clear comments and minimal dependencies.
897. Copilot task: In the Deployment Preparation phase, Write validation
     logic with clear comments and minimal dependencies.
898. Copilot task: In the Deployment Preparation phase, Add Tailwind
     styling with clear comments and minimal dependencies.
899. Copilot task: In the Deployment Preparation phase, Write helper
     function with clear comments and minimal dependencies.
900. Copilot task: In the Deployment Preparation phase, Write unit test
     with clear comments and minimal dependencies.
901. Copilot task: In the Project Setup phase, Create Django model with
     clear comments and minimal dependencies.
902. Copilot task: In the Project Setup phase, Create Django form with
     clear comments and minimal dependencies.
903. Copilot task: In the Project Setup phase, Create Django view with
     clear comments and minimal dependencies.
904. Copilot task: In the Project Setup phase, Create HTML template with
     clear comments and minimal dependencies.
905. Copilot task: In the Project Setup phase, Create URL route with
     clear comments and minimal dependencies.
906. Copilot task: In the Project Setup phase, Add admin registration
     with clear comments and minimal dependencies.
907. Copilot task: In the Project Setup phase, Write validation logic
     with clear comments and minimal dependencies.
908. Copilot task: In the Project Setup phase, Add Tailwind styling with
     clear comments and minimal dependencies.
909. Copilot task: In the Project Setup phase, Write helper function
     with clear comments and minimal dependencies.
910. Copilot task: In the Project Setup phase, Write unit test with
     clear comments and minimal dependencies.
911. Copilot task: In the Django Configuration phase, Create Django
     model with clear comments and minimal dependencies.
912. Copilot task: In the Django Configuration phase, Create Django form
     with clear comments and minimal dependencies.
913. Copilot task: In the Django Configuration phase, Create Django view
     with clear comments and minimal dependencies.
914. Copilot task: In the Django Configuration phase, Create HTML
     template with clear comments and minimal dependencies.
915. Copilot task: In the Django Configuration phase, Create URL route
     with clear comments and minimal dependencies.
916. Copilot task: In the Django Configuration phase, Add admin
     registration with clear comments and minimal dependencies.
917. Copilot task: In the Django Configuration phase, Write validation
     logic with clear comments and minimal dependencies.
918. Copilot task: In the Django Configuration phase, Add Tailwind
     styling with clear comments and minimal dependencies.
919. Copilot task: In the Django Configuration phase, Write helper
     function with clear comments and minimal dependencies.
920. Copilot task: In the Django Configuration phase, Write unit test
     with clear comments and minimal dependencies.
921. Copilot task: In the Marketing Website phase, Create Django model
     with clear comments and minimal dependencies.
922. Copilot task: In the Marketing Website phase, Create Django form
     with clear comments and minimal dependencies.
923. Copilot task: In the Marketing Website phase, Create Django view
     with clear comments and minimal dependencies.
924. Copilot task: In the Marketing Website phase, Create HTML template
     with clear comments and minimal dependencies.
925. Copilot task: In the Marketing Website phase, Create URL route with
     clear comments and minimal dependencies.
926. Copilot task: In the Marketing Website phase, Add admin
     registration with clear comments and minimal dependencies.
927. Copilot task: In the Marketing Website phase, Write validation
     logic with clear comments and minimal dependencies.
928. Copilot task: In the Marketing Website phase, Add Tailwind styling
     with clear comments and minimal dependencies.
929. Copilot task: In the Marketing Website phase, Write helper function
     with clear comments and minimal dependencies.
930. Copilot task: In the Marketing Website phase, Write unit test with
     clear comments and minimal dependencies.
931. Copilot task: In the Authentication System phase, Create Django
     model with clear comments and minimal dependencies.
932. Copilot task: In the Authentication System phase, Create Django
     form with clear comments and minimal dependencies.
933. Copilot task: In the Authentication System phase, Create Django
     view with clear comments and minimal dependencies.
934. Copilot task: In the Authentication System phase, Create HTML
     template with clear comments and minimal dependencies.
935. Copilot task: In the Authentication System phase, Create URL route
     with clear comments and minimal dependencies.
936. Copilot task: In the Authentication System phase, Add admin
     registration with clear comments and minimal dependencies.
937. Copilot task: In the Authentication System phase, Write validation
     logic with clear comments and minimal dependencies.
938. Copilot task: In the Authentication System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
939. Copilot task: In the Authentication System phase, Write helper
     function with clear comments and minimal dependencies.
940. Copilot task: In the Authentication System phase, Write unit test
     with clear comments and minimal dependencies.
941. Copilot task: In the Dashboard UI phase, Create Django model with
     clear comments and minimal dependencies.
942. Copilot task: In the Dashboard UI phase, Create Django form with
     clear comments and minimal dependencies.
943. Copilot task: In the Dashboard UI phase, Create Django view with
     clear comments and minimal dependencies.
944. Copilot task: In the Dashboard UI phase, Create HTML template with
     clear comments and minimal dependencies.
945. Copilot task: In the Dashboard UI phase, Create URL route with
     clear comments and minimal dependencies.
946. Copilot task: In the Dashboard UI phase, Add admin registration
     with clear comments and minimal dependencies.
947. Copilot task: In the Dashboard UI phase, Write validation logic
     with clear comments and minimal dependencies.
948. Copilot task: In the Dashboard UI phase, Add Tailwind styling with
     clear comments and minimal dependencies.
949. Copilot task: In the Dashboard UI phase, Write helper function with
     clear comments and minimal dependencies.
950. Copilot task: In the Dashboard UI phase, Write unit test with clear
     comments and minimal dependencies.
951. Copilot task: In the CMS Models phase, Create Django model with
     clear comments and minimal dependencies.
952. Copilot task: In the CMS Models phase, Create Django form with
     clear comments and minimal dependencies.
953. Copilot task: In the CMS Models phase, Create Django view with
     clear comments and minimal dependencies.
954. Copilot task: In the CMS Models phase, Create HTML template with
     clear comments and minimal dependencies.
955. Copilot task: In the CMS Models phase, Create URL route with clear
     comments and minimal dependencies.
956. Copilot task: In the CMS Models phase, Add admin registration with
     clear comments and minimal dependencies.
957. Copilot task: In the CMS Models phase, Write validation logic with
     clear comments and minimal dependencies.
958. Copilot task: In the CMS Models phase, Add Tailwind styling with
     clear comments and minimal dependencies.
959. Copilot task: In the CMS Models phase, Write helper function with
     clear comments and minimal dependencies.
960. Copilot task: In the CMS Models phase, Write unit test with clear
     comments and minimal dependencies.
961. Copilot task: In the CMS Forms phase, Create Django model with
     clear comments and minimal dependencies.
962. Copilot task: In the CMS Forms phase, Create Django form with clear
     comments and minimal dependencies.
963. Copilot task: In the CMS Forms phase, Create Django view with clear
     comments and minimal dependencies.
964. Copilot task: In the CMS Forms phase, Create HTML template with
     clear comments and minimal dependencies.
965. Copilot task: In the CMS Forms phase, Create URL route with clear
     comments and minimal dependencies.
966. Copilot task: In the CMS Forms phase, Add admin registration with
     clear comments and minimal dependencies.
967. Copilot task: In the CMS Forms phase, Write validation logic with
     clear comments and minimal dependencies.
968. Copilot task: In the CMS Forms phase, Add Tailwind styling with
     clear comments and minimal dependencies.
969. Copilot task: In the CMS Forms phase, Write helper function with
     clear comments and minimal dependencies.
970. Copilot task: In the CMS Forms phase, Write unit test with clear
     comments and minimal dependencies.
971. Copilot task: In the CMS Views phase, Create Django model with
     clear comments and minimal dependencies.
972. Copilot task: In the CMS Views phase, Create Django form with clear
     comments and minimal dependencies.
973. Copilot task: In the CMS Views phase, Create Django view with clear
     comments and minimal dependencies.
974. Copilot task: In the CMS Views phase, Create HTML template with
     clear comments and minimal dependencies.
975. Copilot task: In the CMS Views phase, Create URL route with clear
     comments and minimal dependencies.
976. Copilot task: In the CMS Views phase, Add admin registration with
     clear comments and minimal dependencies.
977. Copilot task: In the CMS Views phase, Write validation logic with
     clear comments and minimal dependencies.
978. Copilot task: In the CMS Views phase, Add Tailwind styling with
     clear comments and minimal dependencies.
979. Copilot task: In the CMS Views phase, Write helper function with
     clear comments and minimal dependencies.
980. Copilot task: In the CMS Views phase, Write unit test with clear
     comments and minimal dependencies.
981. Copilot task: In the Website Instance System phase, Create Django
     model with clear comments and minimal dependencies.
982. Copilot task: In the Website Instance System phase, Create Django
     form with clear comments and minimal dependencies.
983. Copilot task: In the Website Instance System phase, Create Django
     view with clear comments and minimal dependencies.
984. Copilot task: In the Website Instance System phase, Create HTML
     template with clear comments and minimal dependencies.
985. Copilot task: In the Website Instance System phase, Create URL
     route with clear comments and minimal dependencies.
986. Copilot task: In the Website Instance System phase, Add admin
     registration with clear comments and minimal dependencies.
987. Copilot task: In the Website Instance System phase, Write
     validation logic with clear comments and minimal dependencies.
988. Copilot task: In the Website Instance System phase, Add Tailwind
     styling with clear comments and minimal dependencies.
989. Copilot task: In the Website Instance System phase, Write helper
     function with clear comments and minimal dependencies.
990. Copilot task: In the Website Instance System phase, Write unit test
     with clear comments and minimal dependencies.
991. Copilot task: In the Template Rendering phase, Create Django model
     with clear comments and minimal dependencies.
992. Copilot task: In the Template Rendering phase, Create Django form
     with clear comments and minimal dependencies.
993. Copilot task: In the Template Rendering phase, Create Django view
     with clear comments and minimal dependencies.
994. Copilot task: In the Template Rendering phase, Create HTML template
     with clear comments and minimal dependencies.
995. Copilot task: In the Template Rendering phase, Create URL route
     with clear comments and minimal dependencies.
996. Copilot task: In the Template Rendering phase, Add admin
     registration with clear comments and minimal dependencies.
997. Copilot task: In the Template Rendering phase, Write validation
     logic with clear comments and minimal dependencies.
998. Copilot task: In the Template Rendering phase, Add Tailwind styling
     with clear comments and minimal dependencies.
999. Copilot task: In the Template Rendering phase, Write helper
     function with clear comments and minimal dependencies.
1000. Copilot task: In the Template Rendering phase, Write unit test
      with clear comments and minimal dependencies.
